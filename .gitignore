# macOS
.DS_Store

# Node (zero-dependency now, but future-proof)
node_modules/

# Design / moodboard assets — not referenced by code, not versioned
Gemini_Generated_Image_*.jpeg

# Local env / logs
*.log
.env
.env.*

# Agent/editor local state — never shipped, never reviewed
.cursor/

# Browser-MCP capture scratch: regenerated every session, PNGs stay local
e2e-artifacts/
